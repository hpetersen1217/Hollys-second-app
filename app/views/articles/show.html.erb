<h1><%= @article.title %></h1>
<%= link_to "Edit", edit_wiki_article_path(@wiki, @article) %>
<p><% @article.body %></p>


<%= @comments.count %>Comments

<%= render @comments %>

<%# equivalent to above %>
<%# @comments.each %>
  <%#= render partial: 'comments/comment', locals: { comment: @comment } %>
<%# end %>

<%= render partial: 'comments/form', locals: { wiki: @wiki, article: @article, comment: @comment} %>